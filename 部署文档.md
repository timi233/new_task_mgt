# IT服务派工系统 - 生产环境部署文档

## 文档信息

- **系统版本**: 1.0.0
- **部署环境**: 生产环境
- **编写日期**: 2025-12-03
- **最后更新**: 2025-12-16

---

## 一、系统概述

IT服务派工系统是一个基于 Node.js + Vue 3 的全栈应用，用于管理IT服务工单、技术人员派工、客户和渠道信息等。

### 1.1 技术栈

**后端**:
- Node.js 18+ / TypeScript
- Express.js
- Prisma ORM
- SQLite数据库
- 飞书SDK（用于审批集成）

**前端**:
- Vue 3
- Vite
- Element Plus（桌面端）
- Vant（移动端）
- Pinia状态管理

---

## 二、系统要求

### 2.1 硬件要求

- **CPU**: 2核心或以上
- **内存**: 4GB或以上
- **硬盘**: 20GB可用空间
- **网络**: 稳定的内网环境

### 2.2 软件要求

- **操作系统**: Linux (Ubuntu 20.04+推荐) 或 Windows Server
- **Node.js**: 18.x 或以上版本
- **npm**: 9.x 或以上版本
- **Nginx**: 1.18+ (用于前端静态文件服务和反向代理)
- **PM2**: 5.x (推荐用于Node.js进程管理)

### 2.3 网络要求

- 开放端口:
  - 80 (HTTP - Nginx)
  - 3000 (后端API - 仅内网访问)

---

## 三、环境准备

### 3.1 安装Node.js

```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证安装
node --version  # 应显示 v18.x.x
npm --version   # 应显示 9.x.x
```

### 3.2 安装Nginx

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y nginx

# 启动Nginx
sudo systemctl start nginx
sudo systemctl enable nginx

# 验证安装
nginx -v
```

### 3.3 安装PM2 (推荐)

```bash
sudo npm install -g pm2

# 配置PM2开机自启
pm2 startup
# 按照提示执行命令
```

---

## 四、部署步骤

### 4.1 上传部署包

将 `dispatch-system-production.tar.gz` 上传到服务器：

```bash
# 在服务器上创建应用目录
sudo mkdir -p /opt/dispatch-system
cd /opt/dispatch-system

# 上传并解压部署包
# (使用scp或其他方式上传压缩包后)
tar -xzf dispatch-system-production.tar.gz
```

### 4.2 部署后端服务

```bash
cd /opt/dispatch-system/server

# 安装依赖
npm install --production

# 配置环境变量
cp .env.production .env

# 【重要】修改.env文件中的配置
nano .env
# 需要修改的关键配置：
# - JWT_SECRET: 改为随机的强密码
# - FEISHU_APP_ID: 填入实际的飞书应用ID
# - FEISHU_APP_SECRET: 填入实际的飞书应用密钥
# - FEISHU_APPROVAL_CODE: 填入实际的审批模板Code

# 初始化数据库
npx prisma generate
npx prisma db push

# 测试启动（按Ctrl+C停止）
npm start
```

### 4.3 部署前端服务

```bash
cd /opt/dispatch-system/client

# 安装依赖
npm install

# 构建生产版本
npm run build

# 部署到Nginx
sudo rm -rf /var/www/dispatch-system
sudo mkdir -p /var/www/dispatch-system
sudo cp -r dist/* /var/www/dispatch-system/

# 配置Nginx
sudo cp nginx.prod.conf /etc/nginx/sites-available/dispatch-system
sudo ln -s /etc/nginx/sites-available/dispatch-system /etc/nginx/sites-enabled/
sudo rm -f /etc/nginx/sites-enabled/default  # 移除默认配置

# 测试Nginx配置
sudo nginx -t

# 重新加载Nginx
sudo systemctl reload nginx
```

### 4.4 使用PM2启动后端服务

```bash
cd /opt/dispatch-system/server

# 启动服务
pm2 start npm --name "dispatch-backend" -- start

# 保存PM2配置
pm2 save

# 查看服务状态
pm2 status
pm2 logs dispatch-backend
```

---

## 五、配置说明

### 5.1 后端配置 (.env)

部署到新服务器时，**只需修改 `WEB_URL`**，其他配置可保持不变。

```bash
# 服务配置
PORT=3000                    # 后端服务端口
NODE_ENV=production          # 生产环境
WEB_URL=http://你的服务器IP或域名  # 【必须修改】前端访问地址，用于飞书OAuth回调

# 数据库
DATABASE_URL="file:./data/dispatch.db"  # SQLite数据库路径

# JWT
JWT_SECRET=<生成的随机密钥>  # 【必须修改】使用强密码
JWT_EXPIRES_IN=7d

# 飞书应用配置
FEISHU_APP_ID=<你的应用ID>
FEISHU_APP_SECRET=<你的应用密钥>
FEISHU_ENCRYPT_KEY=<加密密钥>
FEISHU_VERIFICATION_TOKEN=<验证令牌>
FEISHU_APPROVAL_CODE=<审批模板Code>
```

### 5.2 Nginx配置

Nginx配置文件位于 `/etc/nginx/sites-available/dispatch-system`。

**配置已优化为通用模式，部署到新服务器时无需修改：**
- `server_name _` - 接受任何域名/IP
- `proxy_pass http://127.0.0.1:3000` - 使用本地回环地址

主要配置项：
- 监听端口：80
- 静态文件路径：/var/www/dispatch-system
- API代理：转发到本地 127.0.0.1:3000

---

## 六、服务管理

### 6.1 后端服务管理 (PM2)

```bash
# 查看状态
pm2 status

# 查看日志
pm2 logs dispatch-backend

# 重启服务
pm2 restart dispatch-backend

# 停止服务
pm2 stop dispatch-backend

# 删除服务
pm2 delete dispatch-backend
```

### 6.2 前端服务管理 (Nginx)

```bash
# 查看状态
sudo systemctl status nginx

# 启动
sudo systemctl start nginx

# 停止
sudo systemctl stop nginx

# 重启
sudo systemctl restart nginx

# 重新加载配置
sudo systemctl reload nginx

# 查看错误日志
sudo tail -f /var/log/nginx/error.log

# 查看访问日志
sudo tail -f /var/log/nginx/access.log
```

---

## 七、部署验证

### 7.1 后端服务验证

```bash
# 检查服务是否运行
pm2 status

# 测试API接口（使用localhost或实际IP）
curl http://localhost:3000/api/health
# 应返回: {"success":true,"message":"OK"}
```

### 7.2 前端服务验证

1. 在浏览器中访问服务器地址
2. 应该能看到登录页面
3. 点击"飞书登录"按钮
4. 使用飞书账号登录
5. 登录成功后应该能看到工作台页面

### 7.3 功能验证

登录后验证以下核心功能：

- ✅ 工作台数据正常加载
- ✅ 创建工单功能正常
- ✅ 查看工单列表
- ✅ 工单派工功能
- ✅ 客户管理功能
- ✅ 渠道管理功能
- ✅ 桌面端/移动端切换正常

---

## 八、常见问题

### 8.1 后端服务无法启动

**问题**: PM2显示服务已停止或报错

**解决方案**:
```bash
# 查看详细日志
pm2 logs dispatch-backend --lines 100

# 常见原因：
# 1. 端口3000被占用
sudo lsof -i :3000
# 2. 数据库文件权限问题
sudo chown -R $USER:$USER /opt/dispatch-system/server/prisma/data
# 3. .env配置错误
nano /opt/dispatch-system/server/.env
```

### 8.2 前端无法访问

**问题**: 浏览器无法打开服务器地址

**解决方案**:
```bash
# 检查Nginx状态
sudo systemctl status nginx

# 检查Nginx配置
sudo nginx -t

# 检查防火墙
sudo ufw status
sudo ufw allow 80/tcp

# 查看Nginx错误日志
sudo tail -f /var/log/nginx/error.log
```

### 8.3 API请求失败

**问题**: 前端能访问，但API请求返回502或504错误

**解决方案**:
```bash
# 1. 检查后端服务是否运行
pm2 status

# 2. 测试后端API
curl http://localhost:3000/api/health

# 3. 检查Nginx代理配置
sudo cat /etc/nginx/sites-available/dispatch-system | grep proxy_pass

# 4. 重启服务
pm2 restart dispatch-backend
sudo systemctl reload nginx
```

### 8.4 飞书登录失败

**问题**: 点击飞书登录后报错或无法跳转

**解决方案**:
1. 检查飞书应用配置是否正确
2. 确认 `.env` 中的 `FEISHU_APP_ID` 和 `FEISHU_APP_SECRET` 正确
3. 确认飞书应用的重定向URL已配置为服务器实际访问地址
4. 确认 `.env` 中的 `WEB_URL` 与飞书应用配置的重定向URL一致
5. 查看后端日志：`pm2 logs dispatch-backend`

---

## 九、安全建议

### 9.1 必须修改的配置

1. **JWT_SECRET**: 使用至少32位随机字符串
   ```bash
   # 生成随机密钥
   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
   ```

2. **数据库文件权限**: 确保只有应用用户可以访问
   ```bash
   chmod 600 /opt/dispatch-system/server/prisma/data/dispatch.db
   ```

3. **环境变量文件权限**:
   ```bash
   chmod 600 /opt/dispatch-system/server/.env
   ```

### 9.2 建议配置

1. 启用HTTPS（如果条件允许）
2. 配置防火墙，只开放必要端口
3. 定期更新依赖包：`npm audit fix`
4. 配置自动备份数据库

---

## 十、升级和回滚

### 10.1 升级步骤

```bash
# 1. 备份数据库
cp /opt/dispatch-system/server/prisma/data/dispatch.db \
   /opt/dispatch-system/server/prisma/data/dispatch.db.backup-$(date +%Y%m%d-%H%M%S)

# 2. 停止服务
pm2 stop dispatch-backend

# 3. 备份代码
cd /opt/dispatch-system
tar -czf ../dispatch-system-backup-$(date +%Y%m%d-%H%M%S).tar.gz .

# 4. 上传并解压新版本
# (使用新的部署包替换现有文件)

# 5. 安装依赖和重新构建
cd /opt/dispatch-system/server
npm install --production
cd /opt/dispatch-system/client
npm install && npm run build
sudo cp -r dist/* /var/www/dispatch-system/

# 6. 运行数据库迁移
cd /opt/dispatch-system/server
npx prisma generate
npx prisma db push

# 7. 重启服务
pm2 restart dispatch-backend
sudo systemctl reload nginx
```

### 10.2 回滚步骤

```bash
# 1. 停止服务
pm2 stop dispatch-backend

# 2. 恢复代码
cd /opt
tar -xzf dispatch-system-backup-<日期>.tar.gz -C dispatch-system

# 3. 恢复数据库
cp /opt/dispatch-system/server/prisma/data/dispatch.db.backup-<日期> \
   /opt/dispatch-system/server/prisma/data/dispatch.db

# 4. 重启服务
pm2 restart dispatch-backend
sudo systemctl reload nginx
```

---

## 十一、联系方式

如遇到部署问题，请联系技术支持团队。

---

**部署完成后，请妥善保管本文档以供后续维护使用。**
