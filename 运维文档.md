# IT服务派工系统 - 运维手册

## 文档信息

- **系统版本**: 1.0.0
- **运维环境**: 生产环境
- **编写日期**: 2025-12-03
- **最后更新**: 2025-12-16

---

## 一、日常运维

### 1.1 服务状态检查

**每日检查清单**:

```bash
# 1. 检查后端服务状态
pm2 status

# 2. 检查前端服务状态
sudo systemctl status nginx

# 3. 检查磁盘空间
df -h

# 4. 检查内存使用
free -h

# 5. 检查进程
top -bn1 | grep node
```

### 1.2 日志查看

**后端日志**:
```bash
# 实时查看后端日志
pm2 logs dispatch-backend

# 查看最近100行日志
pm2 logs dispatch-backend --lines 100

# 查看错误日志
pm2 logs dispatch-backend --err

# 清空日志
pm2 flush
```

**Nginx日志**:
```bash
# 访问日志
sudo tail -f /var/log/nginx/access.log

# 错误日志
sudo tail -f /var/log/nginx/error.log

# 查看最近的错误
sudo grep "error" /var/log/nginx/error.log | tail -20
```

**应用日志**:
```bash
# 后端应用日志位置
/opt/dispatch-system/server/.pm2/logs/

# 查看应用错误日志
tail -f ~/.pm2/logs/dispatch-backend-error.log

# 查看应用输出日志
tail -f ~/.pm2/logs/dispatch-backend-out.log
```

---

## 二、备份与恢复

### 2.1 数据库备份

**自动备份脚本**:

创建备份脚本 `/opt/scripts/backup-database.sh`:

```bash
#!/bin/bash

# 配置
BACKUP_DIR="/opt/backups/database"
DB_PATH="/opt/dispatch-system/server/prisma/data/dispatch.db"
RETENTION_DAYS=30

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份文件名
BACKUP_FILE="dispatch.db.backup-$(date +%Y%m%d-%H%M%S)"

# 执行备份
cp $DB_PATH $BACKUP_DIR/$BACKUP_FILE

# 压缩备份
gzip $BACKUP_DIR/$BACKUP_FILE

# 删除超过保留期的备份
find $BACKUP_DIR -name "dispatch.db.backup-*.gz" -mtime +$RETENTION_DAYS -delete

echo "备份完成: $BACKUP_DIR/$BACKUP_FILE.gz"
```

**设置定时备份**:

```bash
# 赋予执行权限
chmod +x /opt/scripts/backup-database.sh

# 添加到crontab（每天凌晨2点备份）
crontab -e

# 添加以下行
0 2 * * * /opt/scripts/backup-database.sh >> /var/log/db-backup.log 2>&1
```

**手动备份**:

```bash
# 立即备份
/opt/scripts/backup-database.sh

# 或手动复制
cp /opt/dispatch-system/server/prisma/data/dispatch.db \
   /opt/backups/database/dispatch.db.backup-$(date +%Y%m%d-%H%M%S)
```

### 2.2 数据库恢复

```bash
# 1. 停止后端服务
pm2 stop dispatch-backend

# 2. 备份当前数据库（以防万一）
cp /opt/dispatch-system/server/prisma/data/dispatch.db \
   /opt/dispatch-system/server/prisma/data/dispatch.db.before-restore

# 3. 恢复数据库
# 如果备份是压缩的
gunzip -c /opt/backups/database/dispatch.db.backup-<日期>.gz \
   > /opt/dispatch-system/server/prisma/data/dispatch.db

# 如果备份未压缩
cp /opt/backups/database/dispatch.db.backup-<日期> \
   /opt/dispatch-system/server/prisma/data/dispatch.db

# 4. 重启服务
pm2 restart dispatch-backend

# 5. 验证恢复
pm2 logs dispatch-backend
```

### 2.3 代码备份

```bash
# 完整备份整个应用
cd /opt
tar -czf backups/dispatch-system-full-$(date +%Y%m%d-%H%M%S).tar.gz \
   dispatch-system/

# 仅备份配置文件
tar -czf backups/dispatch-system-config-$(date +%Y%m%d-%H%M%S).tar.gz \
   dispatch-system/server/.env \
   dispatch-system/server/prisma/schema.prisma \
   /etc/nginx/sites-available/dispatch-system
```

---

## 三、监控与告警

### 3.1 系统资源监控

**CPU和内存监控**:

```bash
# 查看实时资源使用
htop

# 查看Node进程资源使用
pm2 monit

# 查看详细进程信息
pm2 describe dispatch-backend
```

**磁盘监控**:

```bash
# 查看磁盘使用
df -h

# 查看数据库文件大小
ls -lh /opt/dispatch-system/server/prisma/data/dispatch.db

# 查看日志文件大小
du -sh /var/log/nginx/
du -sh ~/.pm2/logs/
```

### 3.2 服务可用性监控

创建健康检查脚本 `/opt/scripts/health-check.sh`:

```bash
#!/bin/bash

# 检查后端API（使用localhost，无需修改IP）
BACKEND_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/health)

if [ "$BACKEND_STATUS" != "200" ]; then
    echo "[ERROR] 后端服务异常: HTTP $BACKEND_STATUS"
    # 可以在这里添加告警通知
    pm2 restart dispatch-backend
fi

# 检查前端服务（使用localhost，无需修改IP）
FRONTEND_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost/)

if [ "$FRONTEND_STATUS" != "200" ]; then
    echo "[ERROR] 前端服务异常: HTTP $FRONTEND_STATUS"
    sudo systemctl restart nginx
fi

echo "[OK] 所有服务运行正常"
```

**设置定时检查**:

```bash
chmod +x /opt/scripts/health-check.sh

# 每5分钟检查一次
crontab -e
# 添加
*/5 * * * * /opt/scripts/health-check.sh >> /var/log/health-check.log 2>&1
```

### 3.3 日志分析

**错误日志统计**:

```bash
# 统计今天的错误数
sudo grep "$(date +%Y/%m/%d)" /var/log/nginx/error.log | wc -l

# 查看最常见的错误
sudo grep "error" /var/log/nginx/error.log | awk '{print $NF}' | sort | uniq -c | sort -rn | head -10

# PM2错误日志
pm2 logs dispatch-backend --err --lines 50
```

---

## 四、性能优化

### 4.1 数据库优化

**清理历史数据**:

```bash
# 登录到服务器后端目录
cd /opt/dispatch-system/server

# 使用prisma studio查看数据
npx prisma studio

# 创建数据清理脚本（定期清理90天前的已完成工单）
# scripts/cleanup-old-orders.ts
```

**数据库优化**:

```bash
# SQLite数据库优化
sqlite3 /opt/dispatch-system/server/prisma/data/dispatch.db "VACUUM;"
sqlite3 /opt/dispatch-system/server/prisma/data/dispatch.db "ANALYZE;"
```

### 4.2 日志管理

**日志轮转配置**:

创建 `/etc/logrotate.d/dispatch-system`:

```
/var/log/nginx/access.log /var/log/nginx/error.log {
    daily
    missingok
    rotate 14
    compress
    delaycompress
    notifempty
    create 0640 www-data adm
    sharedscripts
    postrotate
        if [ -f /var/run/nginx.pid ]; then
            kill -USR1 `cat /var/run/nginx.pid`
        fi
    endscript
}

/root/.pm2/logs/*.log {
    daily
    missingok
    rotate 7
    compress
    notifempty
    create 0640 root root
    postrotate
        pm2 reloadLogs
    endscript
}
```

**清理PM2日志**:

```bash
# 清空所有日志
pm2 flush

# 重新加载日志文件
pm2 reloadLogs
```

---

## 五、故障处理

### 5.1 服务无响应

**后端服务无响应**:

```bash
# 1. 查看进程状态
pm2 status

# 2. 查看日志
pm2 logs dispatch-backend --lines 100

# 3. 重启服务
pm2 restart dispatch-backend

# 4. 如果重启失败，强制重启
pm2 delete dispatch-backend
cd /opt/dispatch-system/server
pm2 start npm --name "dispatch-backend" -- start
pm2 save
```

**前端服务无响应**:

```bash
# 1. 检查Nginx状态
sudo systemctl status nginx

# 2. 测试配置
sudo nginx -t

# 3. 重启Nginx
sudo systemctl restart nginx

# 4. 检查端口占用
sudo netstat -tulpn | grep :80
```

### 5.2 数据库问题

**数据库锁定**:

```bash
# 1. 停止所有访问数据库的服务
pm2 stop dispatch-backend

# 2. 检查数据库完整性
sqlite3 /opt/dispatch-system/server/prisma/data/dispatch.db "PRAGMA integrity_check;"

# 3. 重启服务
pm2 restart dispatch-backend
```

**数据库损坏**:

```bash
# 1. 停止服务
pm2 stop dispatch-backend

# 2. 备份损坏的数据库
cp /opt/dispatch-system/server/prisma/data/dispatch.db \
   /opt/dispatch-system/server/prisma/data/dispatch.db.corrupted

# 3. 尝试修复
sqlite3 /opt/dispatch-system/server/prisma/data/dispatch.db ".recover" | \
   sqlite3 /opt/dispatch-system/server/prisma/data/dispatch.db.recovered

# 4. 如果修复失败，从备份恢复
# （参考 2.2 数据库恢复）
```

### 5.3 内存泄漏

**检测内存泄漏**:

```bash
# 查看Node进程内存使用
pm2 show dispatch-backend

# 如果内存持续增长，重启服务
pm2 restart dispatch-backend

# 设置自动重启（当内存超过500MB时）
pm2 start npm --name "dispatch-backend" -- start --max-memory-restart 500M
pm2 save
```

### 5.4 磁盘空间不足

```bash
# 1. 查看磁盘使用
df -h

# 2. 查找大文件
du -h /opt/dispatch-system | sort -rh | head -20

# 3. 清理日志
pm2 flush
sudo journalctl --vacuum-size=100M

# 4. 清理旧备份
find /opt/backups -mtime +60 -delete

# 5. 清理npm缓存
npm cache clean --force
```

---

## 六、安全维护

### 6.1 系统更新

```bash
# 更新系统包
sudo apt update
sudo apt upgrade -y

# 更新Node.js依赖（谨慎操作）
cd /opt/dispatch-system/server
npm audit
npm audit fix

cd /opt/dispatch-system/client
npm audit
npm audit fix
```

### 6.2 访问控制

```bash
# 检查文件权限
ls -la /opt/dispatch-system/server/.env
ls -la /opt/dispatch-system/server/prisma/data/dispatch.db

# 确保只有应用用户可以访问
chmod 600 /opt/dispatch-system/server/.env
chmod 600 /opt/dispatch-system/server/prisma/data/dispatch.db
```

### 6.3 防火墙配置

```bash
# 检查防火墙状态
sudo ufw status

# 只允许必要的端口
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP
sudo ufw enable
```

---

## 七、定期维护任务

### 7.1 每日任务

- ✅ 检查服务状态（PM2、Nginx）
- ✅ 检查磁盘空间和内存使用
- ✅ 查看错误日志

### 7.2 每周任务

- ✅ 分析访问日志，统计系统使用情况
- ✅ 检查数据库大小和增长趋势
- ✅ 验证备份是否正常
- ✅ 清理临时文件和日志

### 7.3 每月任务

- ✅ 数据库优化（VACUUM、ANALYZE）
- ✅ 系统安全更新
- ✅ 检查SSL证书到期时间（如果使用HTTPS）
- ✅ 审查用户权限和访问日志
- ✅ 进行一次数据库恢复演练

### 7.4 每季度任务

- ✅ 全面性能评估
- ✅ 容量规划评估
- ✅ 灾难恢复演练
- ✅ 安全审计

---

## 八、监控指标

### 8.1 关键性能指标 (KPI)

- **系统可用性**: 目标 99.5%以上
- **API响应时间**: 平均 < 200ms
- **数据库查询时间**: 平均 < 50ms
- **页面加载时间**: < 2秒
- **并发用户数**: 支持 50+

### 8.2 告警阈值

- CPU使用率 > 80% 持续5分钟
- 内存使用率 > 85%
- 磁盘使用率 > 90%
- API错误率 > 5%
- 服务不可用 > 1分钟

---

## 九、应急联系

### 9.1 技术支持

- **技术支持团队**: [团队联系方式]
- **紧急联系电话**: [电话]
- **邮箱**: [support@example.com]

### 9.2 供应商联系

- **服务器供应商**: [联系方式]
- **网络服务商**: [联系方式]

---

## 十、附录

### 10.1 常用命令速查

```bash
# 服务管理
pm2 status                          # 查看服务状态
pm2 restart dispatch-backend        # 重启后端
pm2 logs dispatch-backend           # 查看日志
sudo systemctl restart nginx        # 重启Nginx

# 日志查看
tail -f /var/log/nginx/error.log    # Nginx错误日志
pm2 logs --lines 100                # PM2日志
sudo journalctl -u nginx -f         # 系统日志

# 系统监控
htop                                # 进程监控
df -h                               # 磁盘使用
free -h                             # 内存使用
netstat -tulpn                      # 端口监听

# 数据库
sqlite3 dispatch.db ".tables"       # 查看表
sqlite3 dispatch.db "SELECT COUNT(*) FROM work_orders;"  # 查询数据
```

### 10.2 重要文件路径

```
应用目录: /opt/dispatch-system/
后端代码: /opt/dispatch-system/server/
前端代码: /opt/dispatch-system/client/
数据库: /opt/dispatch-system/server/prisma/data/dispatch.db
后端配置: /opt/dispatch-system/server/.env
Nginx配置: /etc/nginx/sites-available/dispatch-system
前端静态文件: /var/www/dispatch-system/
备份目录: /opt/backups/
脚本目录: /opt/scripts/
```

---

**请定期复习本手册，确保熟悉所有运维流程。**
