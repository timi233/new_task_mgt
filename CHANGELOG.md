
## 2025-12-16

### 新增
- **工单列表描述字段**：桌面端工单管理页面表格新增"描述"列，显示在"客户"与"类型"之间

### 优化
- **部署配置灵活化**：Nginx配置移除硬编码IP，使用通配符`server_name _`和本地回环地址`127.0.0.1`，部署到新服务器时只需修改`.env`中的`WEB_URL`
- **数据库索引优化**：为工单表添加复合索引（status/orderType/priority/createdAt、submitterId、relatedSalesId），为技术员关联表添加technicianId索引，提升查询性能
- **统计API内存优化**：概览统计使用distinct查询替代全量数据加载，避免大数据量时内存溢出
- **附件访问安全增强**：附件URL由服务端生成短期签名Token（5分钟有效），替代长期JWT，降低Token泄露风险

### 安全修复
- **JWT_SECRET 启动检查**：生产环境启动时强制校验 JWT_SECRET 是否配置，防止使用默认密钥
- **飞书回调验签**：新增飞书事件回调签名验证，防止伪造审批回调攻击
- **工单横向越权修复**：工单详情、接单、开始、完成、取消、评价接口增加用户权限校验，非管理员只能操作自己相关的工单
- **日志脱敏处理**：客户/渠道/厂家对接人保存失败时，日志中姓名和电话自动脱敏（如"张***"、"138****56"）
- **工单编号竞态修复**：创建工单时添加重试机制（最多5次），防止并发创建时编号冲突导致失败
- **HttpOnly Cookie认证**：登录Token改用HttpOnly Cookie存储，防止XSS攻击窃取凭证；移除localStorage存储

## 2025-12-15

### 新增
- **审批表单外勤类型字段**：飞书审批表单新增外勤类型字段，根据工单类型自动填写"公司外勤"或"厂家外勤"
- **厂家派工实际服务时间**：厂家派工页面新增实际服务时间显示，工单未结束显示"工单暂未结束"，结束后显示实际天数
- **厂家派工导出增强**：导出 CSV 新增实际服务时间字段

## 2025-12-09

### 修复
- **附件下载认证失败问题**：后端 `authenticate` 中间件支持从 URL query 参数 `?token=xxx` 获取 token，解决直接访问附件链接时无法携带 Authorization header 的问题
- **中文文件名乱码问题**：Multer 上传时将 `originalname` 从 latin1 转换为 UTF-8 编码

### 新增
- **文件名日期后缀**：上传文件自动添加 `_YYYYMMDD` 后缀，如 `报告.xlsx` → `报告_20251209.xlsx`

### 数据修正
- 修正数据库中乱码文件名 `æºæä½ç½®å¾.xlsx` → `任丘机柜位置图_20251209.xlsx`
