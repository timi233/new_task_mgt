# 派工系统权限管理文档

## 更新日期
2024-12-04

## 权限优化总结

### 1. 无权限用户访问控制 ✅

所有无权限用户（functionalRole 和 responsibilityRole 都为 null 或 OTHER）：
- 工单管理、客户管理、渠道管理：返回空列表
- 详情页面：返回 403 权限不足

---

### 2. 工单管理权限 ✅

| 用户类型         | 权限                |
|--------------|-------------------|
| 系统管理员/管理员/审计 | 查看和管理所有工单         |
| 技术员          | 只能查看和管理分配给自己的工单   |
| 销售           | 只能查看和管理自己提交或关联的工单 |

**实现位置：** `/server/src/routes/workorder.ts`

---

### 3. 客户管理权限 ✅

| 用户类型         | 权限               |
|--------------|------------------|
| 系统管理员/管理员/审计 | 查看和管理所有客户        |
| 技术员          | 只能查看和管理自己工单相关的客户 |
| 销售           | 只能查看和管理自己工单相关的客户 |

**实现位置：** `/server/src/routes/customer.ts`

---

### 4. 渠道管理权限 ✅

| 用户类型         | 权限               |
|--------------|------------------|
| 系统管理员/管理员/审计 | 查看和管理所有渠道        |
| 技术员          | 只能查看和管理自己工单相关的渠道 |
| 销售           | 只能查看和管理自己工单相关的渠道 |

**实现位置：** `/server/src/routes/channel.ts`

---

### 5. 派工总览权限 ✅（重点优化）

**访问权限：** 所有有业务权限的用户都可以访问（技术员、销售、管理员、审计）

**数据分级显示：**

| 用户类型         | 看到的数据                             |
|--------------|-----------------------------------|
| 系统管理员/管理员/审计 | 所有人的完整派工信息                        |
| 技术员          | 自己的完整信息 + 其他人的姓名（客户信息隐藏为 ***）     |
| 销售           | 自己相关工单的完整信息 + 其他人的姓名（客户信息隐藏为 ***） |

**实现位置：** `/server/src/routes/schedule.ts`

---

### 6. 统计报表权限 ✅（重点优化）

| 用户类型         | 可访问的 Scope     | 数据范围                     |
|--------------|----------------|--------------------------|
| 系统管理员/管理员/审计 | my 和 overview  | 全部数据                     |
| 技术员          | 仅 my（我的数据）     | 只看自己相关的工单统计              |
| 销售           | 仅 overview（总览） | 只看自己相关的工单统计（不管选什么 scope） |

**前端调整：**
- 技术员访问统计页面时，隐藏 "总览" tab
- 销售访问统计页面时，隐藏 "我的数据" tab

**实现位置：**
- 后端：`/server/src/routes/statistics.ts`
- 前端：需要根据用户角色隐藏相应 tab

---

### 7. 前端路由权限 ✅

更新了 `/client/src/router/index.ts` 中的 `canAccessSchedule` 函数：
- 现在所有有业务权限的用户（技术员、销售、管理员、审计）都可以访问派工总览

**实现位置：** `/client/src/router/index.ts`

---

### 8. 新增权限辅助函数 ✅

在 `/server/src/types/enums.ts` 中新增：

| 函数名 | 说明 |
|-------|------|
| `isAuditor()` | 判断是否是审计 |
| `hasBusinessRole()` | 判断是否有业务权限（技术/销售） |
| `hasManagementRole()` | 判断是否有管理权限（系统管理员/管理员/审计） |
| `hasAnyPermission()` | 判断是否有任何有效权限 |
| `hasNoPermission()` | 判断是否完全无权限 |

**实现位置：** `/server/src/types/enums.ts`

---

## 测试指南

### 1. 创建测试用户

建议创建以下几种用户进行测试：

```sql
-- 无权限用户
UPDATE "User" SET "functionalRole" = NULL, "responsibilityRole" = NULL WHERE name = '测试用户1';

-- 普通技术员
UPDATE "User" SET "functionalRole" = 'TECHNICIAN', "responsibilityRole" = NULL WHERE name = '测试用户2';

-- 普通销售
UPDATE "User" SET "functionalRole" = 'SALES', "responsibilityRole" = NULL WHERE name = '测试用户3';

-- 审计
UPDATE "User" SET "functionalRole" = NULL, "responsibilityRole" = 'AUDITOR' WHERE name = '测试用户4';
```

### 2. 测试场景

- ✅ 无权限用户登录后，所有页面都应该是空数据
- ✅ 技术员只能看到分配给自己的工单和相关客户/渠道
- ✅ 销售只能看到自己提交/关联的工单和相关客户/渠道
- ✅ 派工总览：技术员和销售能看到所有人的时间安排，但其他人的客户信息被隐藏
- ✅ 统计报表：技术员只有"我的数据"，销售只有"总览数据"

---

## 权限验证流程

### 后端权限验证

```typescript
// 1. 从 token 中获取用户信息
const user = req.user;

// 2. 检查用户是否有权限
if (hasNoPermission(user.functionalRole, user.responsibilityRole)) {
  return res.status(403).json({ message: '无权限访问' });
}

// 3. 根据角色过滤数据
if (hasManagementRole(user.responsibilityRole)) {
  // 管理员可以看到所有数据
  // 查询所有数据
} else if (user.functionalRole === FunctionalRole.TECHNICIAN) {
  // 技术员只能看到分配给自己的工单
  // 添加 technicianId 过滤条件
} else if (user.functionalRole === FunctionalRole.SALES) {
  // 销售只能看到自己提交的工单
  // 添加 submitterId 过滤条件
}
```

### 前端路由守卫

```typescript
// /client/src/router/index.ts
router.beforeEach((to, from, next) => {
  const user = getUserFromToken();

  // 检查是否有访问权限
  if (to.meta.requiresAuth) {
    if (!hasAnyPermission(user)) {
      next('/login');
      return;
    }

    // 检查派工总览权限
    if (to.path === '/schedule' && !canAccessSchedule(user)) {
      next('/forbidden');
      return;
    }
  }

  next();
});
```

---

## 关键文件清单

### 后端文件

| 文件路径 | 说明 |
|---------|------|
| `/server/src/types/enums.ts` | 权限辅助函数定义 |
| `/server/src/routes/workorder.ts` | 工单权限控制 |
| `/server/src/routes/customer.ts` | 客户权限控制 |
| `/server/src/routes/channel.ts` | 渠道权限控制 |
| `/server/src/routes/schedule.ts` | 派工总览权限控制 |
| `/server/src/routes/statistics.ts` | 统计报表权限控制 |

### 前端文件

| 文件路径 | 说明 |
|---------|------|
| `/client/src/router/index.ts` | 路由权限守卫 |
| `/client/src/stores/user.ts` | 用户状态管理 |

---

## 常见问题

### Q1: 为什么某些用户看不到任何数据？

**A:** 检查该用户的 `functionalRole` 和 `responsibilityRole` 是否都为 `null` 或 `OTHER`。如果是，则该用户被视为无权限用户，系统会返回空列表。

### Q2: 技术员为什么看不到其他人的客户信息？

**A:** 这是权限设计的一部分。技术员只能看到自己负责的工单相关的客户信息。如果需要查看其他客户，请联系管理员。

### Q3: 派工总览中为什么其他人的客户信息显示为 ***？

**A:** 为了保护客户隐私，技术员和销售在查看派工总览时，只能看到自己相关工单的完整客户信息，其他人的客户信息会被隐藏。

### Q4: 如何给用户添加权限？

**A:** 通过更新数据库中的用户记录：

```sql
-- 设置为技术员
UPDATE "User" SET "functionalRole" = 'TECHNICIAN' WHERE name = '用户名';

-- 设置为销售
UPDATE "User" SET "functionalRole" = 'SALES' WHERE name = '用户名';

-- 设置为管理员
UPDATE "User" SET "responsibilityRole" = 'MANAGER' WHERE name = '用户名';

-- 设置为审计
UPDATE "User" SET "responsibilityRole" = 'AUDITOR' WHERE name = '用户名';

-- 设置为系统管理员
UPDATE "User" SET "responsibilityRole" = 'SYSTEM_ADMIN' WHERE name = '用户名';
```

---

## 版本历史

### v1.0 (2024-12-04)
- ✅ 实现完整的权限管理体系
- ✅ 新增权限辅助函数
- ✅ 优化派工总览权限
- ✅ 优化统计报表权限
- ✅ 更新前端路由权限守卫
